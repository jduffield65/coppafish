; This ini file specifies the minimum number of params i.e. all params which have no default
; It is the same as simplest_settings.ini except in a section (omp) for which page does not exist when testing and
; section which can be different (file_names).

[file_names]
input_dir = input_dirDIFFERENT
output_dir = output_dirDIFFERENT
tile_dir = tile_dirDIFFERENT
code_book = code_bookDIFFERENT

[basic_info]
is_3d = True

[omp]
dp_thresh = 0.578929523
